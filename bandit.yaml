exclude_dirs:
  - tests
  - migrations
  - alembic
  - .venv
  - .pytest_cache

skips:
  - B101  # Test for use of assert (we use assert in tests)
  - B601  # Test for shell injection (parameterized shell commands)

confidence:
  - high
  - medium

level:
  - high
  - medium
